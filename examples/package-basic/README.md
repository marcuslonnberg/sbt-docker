Example using package task
==========================

Build the image by running `sbt docker` in this directory.

Then run the produced image with `docker run -i sbtdocker/example-package-basic:v0.1.0`.
